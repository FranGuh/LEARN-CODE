> “Uso === para evitar coerción implícita; == solo para nullish checks.”

> “Object.is existe para casos de comparación interna del motor o librerías, no para uso cotidiano.”

> “Los primitivos son inmutables; cuando ‘cambias’ el valor, se crea uno nuevo.”

> “Para el motor, una arrow function asignada a const es solo una variable hasta la fase de ejecución.”